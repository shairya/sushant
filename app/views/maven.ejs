<html>
    <head><title>Unicommerce - <%=title%></title></head>
    <link rel="stylesheet" type="text/css" href="/css/style.css" />
    <body>

        <h3>Unicommerce - Maven</h3>
        <hr/>
        <table class="tableClass" cellspacing="30">
            <tr>
                <th colspan="5">Import</th>
            </tr>
            <tr>
                <td><a href="/scrapeorder/secretwish">Secretwish</a></td>
                <td><a href="/scrapeorder/gps">Grandpitstop</a></td>
                <td><a href="/scrapeorder/jerado">Jerado</a></td>
                <td><a href="/scrapeorder/markmediums">Markmediums</a></td>
                <td><a href="/scrapeorder/anscommerce">Anscommerce</a></td>
            </tr>
        </table>
        <table class="tableClass" cellspacing="30">
                <tr>
                    <th colspan="5">Export</th>
                </tr>
                <tr>
                    <td>
                        <% if(secretwishCount==0) {%>
                            Secretwish (<%=secretwishCount%>)
                        <% } else{ %>
                            <a href="/mavenpush/secretwish">Secretwish</a> (<%=secretwishCount%>)
                        <% } %>
                    </td>
                    <td>
                        <% if(gpsCount==0) {%>
                            Grandpitstop (<%=gpsCount%>)
                        <% } else{ %>
                            <a href="/mavenpush/gps">GPS</a> (<%=gpsCount%>)
                        <% } %>
                    </td>
                    <td>
                        <% if(jeradoCount==0) {%>
                            Jerado (<%=jeradoCount%>)
                        <% } else{ %>
                            <a href="/mavenpush/jerado">Jerado</a> (<%=jeradoCount%>)
                        <% } %>
                    </td>
                    <td>
                        <% if(markmediumsCount==0) {%>
                            Markmediums (<%=markmediumsCount%>)
                        <% } else{ %>
                            <a href="/mavenpush/markmediums">Markmediums</a> (<%=markmediumsCount%>)
                        <% } %>
                    </td>
                    <td>
                        <% if(anscommerceCount==0) {%>
                            AnsCommerce (<%=anscommerceCount%>)
                        <% } else{ %>
                            <a href="/mavenpush/anscommerce">Anscommerce</a> (<%=anscommerceCount%>)
                        <% } %>
                    </td>
                </tr>
            </table>
    </body>
</html>